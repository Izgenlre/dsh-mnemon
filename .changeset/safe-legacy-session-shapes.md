---
"dsh-mnemon": patch
---

Extend explicit legacy Session copy repair to the historical Runtime memory snapshot summary, compatible closed-schema v2 subagent descriptors, and packed tool deltas with empty string IDs or names. Normalize null delta names to empty strings while preserving the property, assembly and token timing. Preserve other plugins and original artifacts, and report incompatible delta shapes or empty durable tool identities without publishing a partial copy. Verify repaired history and timed stream replay with the published DSH migration.
