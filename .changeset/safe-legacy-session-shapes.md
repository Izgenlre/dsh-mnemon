---
"dsh-mnemon": patch
---

Extend explicit legacy Session copy repair to the historical Runtime memory snapshot summary, compatible closed-schema v2 subagent descriptors, and packed tool deltas with empty string IDs or names. Normalize null delta names while preserving assembly and token timing, and restore a previously recorded DeepSeek provider ID only when the complete stream and tool call/result chain establish that identity uniquely. Preserve other plugins and original artifacts, refuse ambiguous or unaccounted identity references, and verify repaired history, timed replay and continuation with published DSH migration and the actual WebUI.
